<div class="modal fade" id="inProgressModal" tabindex="-1" role="dialog" aria-labelledby="inProgressModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h2>Malheureusement indisponible !</h2>
        Merci infiniment pour votre implication ! <br>
        Malheureusement, cette fonctionnalité n'est pas encore disponible ! <br>
        Je vous invite à vous inscire à ma Lettre d'Infos pour être informé des prochaines évolutions de la plate-forme.<br><br>
        <% if current_user %>
          <div class="form-actions">
            <br>
            <%= link_to "La Lettre d'Infos", newsletter_path, class: "btn btn-default" %>
          </div>
        <% else %>
          <%= render :template => "devise/sessions/new" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
