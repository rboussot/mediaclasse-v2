<div class="home-wrapper">
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-6 col-xs-offset-3"><br><br>

        <h2>Connexion</h2>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email, required: false, autofocus: true %>
            <%= f.input :password, label: "Mot de passe", required: false %>
            <%= f.input :remember_me, label: "se souvenir de moi",  as: :boolean if devise_mapping.rememberable? %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Me connecter" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
        <br><br><br><p style="color:red"><i>PS : Pour garder la plus parfaite confidentialité, tous les mots de passe ont été réinitialisés avec la nouvelle plate-forme. <br>
        Pour votre première connexion, je vous invite à cliquer sur "Réinitialiser mon mot de passe" afin de choisir votre nouveau mot de passe.</i></p>

      </div>
    </div>
  </div>
</div>
