<%= form_tag course_like_path(course), remote: true do %>
  <%= label_tag "liked", "   Garder dans Mes Cours ?" %>
  <%= check_box_tag :liked, "1", current_user.has_course?(course), class: "like-link" %>
<% end %>
